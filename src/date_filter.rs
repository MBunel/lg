//use crate::filter::Compare;
//use crate::header::MyHeader;

use chrono::{Date, Utc};

#[derive(Debug)]
pub struct DateFilter {
    //operator: String,
    //date: Date<Utc>,
}

impl DateFilter {
    // pub fn new(input_string: &String, attribute_name: &String) -> DateFilter {}
}
